{
  "name": "aif-request",
  "version": "0.0.3",
  "description": "Simple methods to make data requests to a web server using the POST, GET methods, and the data encryption with AES algorithm.",
  "author": {
    "name": "Martin R. Mondragon Sotelo",
    "email": "mygnet@gmail.com"
  },
  "main": "dist/index.js",
  "scripts": {
    "test": "node ./test",
    "build": "babel src -d dist && node ./pack",
    "lint": "eslint ./",
    "lint:fix": "eslint ./ --fix",
    "precommit": "lint-staged"
  },
  "homepage": "https://github.com/mygnet/aif-request",
  "repository": {
    "type": "git",
    "url": "git://github.com/mygnet/aif-request.git"
  },
  "bugs": {
    "url": "https://github.com/mygnet/aif-request/issues"
  },
  "license": "MIT",
  "private": false,
  "keywords": [
    "request",
    "send",
    "post",
    "http",
    "https",
    "download",
    "submit",
    "upload",
    "json",
    "response"
  ],
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-minify": "^0.5.1"
  },
  "dependencies": {
    "aif-cipher": "^0.0.2",
    "form-data": "^3.0.0"
  }
}
